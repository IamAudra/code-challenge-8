import { data }  from "./data.mjs";

const { name } = data

const { location } = data

const {education} = data

const {hobbies} = data

const {baking} = data

const { movies} = data

console.log(name)
console.log(education)
console.log(location);
console.log(hobbies);
console.log(baking);
console.log(movies);